<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/head") %>
    <title>Login</title>
    <link rel="stylesheet" href="/styles/setPassword.css" />
  </head>
  <body>
    <header>阿良購物註冊</header>
    <section>
      <div class="username-input">
        <p>使用者名稱：</p>
        <input type="text" name="username" class="username" required />
      </div>
      <div class="email-input">
        <p>信箱：</p>
        <input type="email" name="email" class="email" required />
      </div>
      <div class="err-email-msg"></div>
      <div class="setPassword">
        <div class="password-input">
          <p>密碼：</p>
          <input type="password" name="password" class="password" required />
        </div>
        <div class="passwordMsg"></div>
        <div class="password-input">
          <p>確認密碼：</p>
          <input
            type="password"
            name="checkPassword"
            class="checkPassword"
            required
          />
        </div>
      </div>

      <button class="btn" disabled>確認送出</button>
    </section>
    <script src="/signup/setPassword.js"></script>
  </body>
</html>
